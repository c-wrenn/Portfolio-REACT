// Import Navigation 
import Navigation from "./Navigation.jsx";

// Header component
const Header = () => {
  return (
    <header>
      {/* Navigation links */}
      < Navigation />
    </header>
  );
};

export default Header;